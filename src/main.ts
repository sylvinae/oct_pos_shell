// This file is not used since we load the React app from localhost:5173
// The API injection happens in inject-apis.js which gets loaded into the React app's webview

console.log("Tauri shell main.ts loaded (but not used for React app)");
